{
  "name": "squarehit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "pnpm build --base squarehit && pnpm gh-pages -d dist",
    "test": "vitest src"
  },
  "dependencies": {
    "preact": "^10.11.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "gh-pages": "^5.0.0",
    "typescript": "^4.9.5",
    "vite": "^4.1.0",
    "vitest": "^0.28.5"
  }
}
